# Demo workspace

This intentionally tiny, read-only workspace is mounted into the Agent service.
It is a safe target for inspecting repository files during the deterministic
demo. Use a disposable writable workspace for real coding tasks.
